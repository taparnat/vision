#codes for processing big data in agriculture from MOOC 
#download some type of data for studying which has a huuuge size 
import opencv #for 3D images
import matplotlib.pyplot as plt #for plotting 
from nicegui import ui #for frontend UI 
import pandas #for datafraame
import numpy #for arrays 
#map function in blocks of big data: synatx - map(function, collection)
from math import sqrt
roots = map(sqrt, (2,3,4,5,6,7)
print (roots)
#when there are more collections, map still works the same map(function, collection_1, collection_2)
from math import mean
means = map(mean, (2,3,4), (8,9,2), (5, 2, 9))


            
